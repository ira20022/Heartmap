import React from 'react';
import PartnerCompatibilityClient from './components/PartnerCompatibilityClient';

export const metadata = {
  title: 'Partner Compatibility — LovePersona',
  description: 'Discover how two Love Personas match with AI-analyzed relationship dynamics.',
};

export default function PartnerCompatibilityPage() {
  return <PartnerCompatibilityClient />;
}
